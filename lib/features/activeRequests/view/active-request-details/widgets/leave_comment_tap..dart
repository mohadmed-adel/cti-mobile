import 'package:flutter/material.dart';

class LeaveCommentTap extends StatelessWidget {
  const LeaveCommentTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
